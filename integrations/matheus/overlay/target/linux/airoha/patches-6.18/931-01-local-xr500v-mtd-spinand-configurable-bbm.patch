From: Cristian Papa <76759272+Cris7015@users.noreply.github.com>
Subject: [PATCH] mtd: spinand: make bad-block marker layout configurable

The SPI-NAND core currently assumes that every device uses a two-byte bad
block marker in the first page of an eraseblock.  Some devices instead use
a one-byte marker and may place a factory marker in either of the first two
pages.

Add per-chip flags for the marker width and second-page policy while
keeping the existing two-byte, first-page behaviour as the default.  When
marking a device with a two-page policy, attempt both pages and retain the
first programming error.  Leave bit 5 unused because later kernels assign
it to SPINAND_ODTR_PACKED_PAGE_READ.

Signed-off-by: Cristian Papa <76759272+Cris7015@users.noreply.github.com>
---
--- a/include/linux/mtd/spinand.h
+++ b/include/linux/mtd/spinand.h
@@ -417,6 +417,8 @@ struct spinand_ecc_info {
 #define SPINAND_HAS_PROG_PLANE_SELECT_BIT		BIT(2)
 #define SPINAND_HAS_READ_PLANE_SELECT_BIT		BIT(3)
 #define SPINAND_NO_RAW_ACCESS				BIT(4)
+#define SPINAND_BBM_1_BYTE				BIT(6)
+#define SPINAND_BBM_SECOND_PAGE				BIT(7)
 
 /**
  * struct spinand_ondie_ecc_conf - private SPI-NAND on-die ECC engine structure
--- a/drivers/mtd/nand/spi/core.c
+++ b/drivers/mtd/nand/spi/core.c
@@ -968,74 +968,106 @@ static int spinand_mtd_write(struct mtd_
 	return ret;
 }
 
+static unsigned int spinand_bbm_len(const struct spinand_device *spinand)
+{
+	return spinand->flags & SPINAND_BBM_1_BYTE ? 1 : 2;
+}
+
+static unsigned int spinand_bbm_npages(const struct spinand_device *spinand)
+{
+	return spinand->flags & SPINAND_BBM_SECOND_PAGE ? 2 : 1;
+}
+
 static bool spinand_isbad(struct nand_device *nand, const struct nand_pos *pos)
 {
 	struct spinand_device *spinand = nand_to_spinand(nand);
 	u8 marker[2] = { };
 	struct nand_page_io_req req = {
 		.pos = *pos,
-		.ooblen = sizeof(marker),
 		.ooboffs = 0,
 		.oobbuf.in = marker,
 		.mode = MTD_OPS_RAW,
 	};
-	int ret;
+	unsigned int marker_len = spinand_bbm_len(spinand);
+	unsigned int npages = spinand_bbm_npages(spinand);
+	int ret, i, j;
 
-	spinand_select_target(spinand, pos->target);
+	ret = spinand_select_target(spinand, pos->target);
+	if (ret)
+		return true;
 
-	ret = spinand_read_page(spinand, &req);
-	if (ret == -EOPNOTSUPP) {
-		/* Retry with ECC in case raw access is not supported */
-		req.mode = MTD_OPS_PLACE_OOB;
-		spinand_read_page(spinand, &req);
-	}
+	for (i = 0; i < npages; i++) {
+		req.pos.page = i;
+		req.ooblen = marker_len;
+		req.mode = MTD_OPS_RAW;
+		memset(marker, 0, sizeof(marker));
+
+		ret = spinand_read_page(spinand, &req);
+		if (ret == -EOPNOTSUPP) {
+			/* Retry with ECC in case raw access is not supported */
+			req.mode = MTD_OPS_PLACE_OOB;
+			ret = spinand_read_page(spinand, &req);
+		}
+		if (ret < 0)
+			return true;
 
-	if (marker[0] != 0xff || marker[1] != 0xff)
-		return true;
+		for (j = 0; j < marker_len; j++)
+			if (marker[j] != 0xff)
+				return true;
+	}
 
 	return false;
 }
 
 static int spinand_mtd_block_isbad(struct mtd_info *mtd, loff_t offs)
 {
 	struct nand_device *nand = mtd_to_nanddev(mtd);
 	struct spinand_device *spinand = nand_to_spinand(nand);
 	struct nand_pos pos;
 	int ret;
 
 	nanddev_offs_to_pos(nand, offs, &pos);
 	mutex_lock(&spinand->lock);
 	ret = nanddev_isbad(nand, &pos);
 	mutex_unlock(&spinand->lock);
 
 	return ret;
 }
 
 static int spinand_markbad(struct nand_device *nand, const struct nand_pos *pos)
 {
 	struct spinand_device *spinand = nand_to_spinand(nand);
 	u8 marker[2] = { };
 	struct nand_page_io_req req = {
 		.pos = *pos,
 		.ooboffs = 0,
-		.ooblen = sizeof(marker),
 		.oobbuf.out = marker,
 		.mode = MTD_OPS_RAW,
 	};
-	int ret;
+	unsigned int marker_len = spinand_bbm_len(spinand);
+	unsigned int npages = spinand_bbm_npages(spinand);
+	int ret, res, i;
 
 	ret = spinand_select_target(spinand, pos->target);
 	if (ret)
 		return ret;
 
-	ret = spinand_write_page(spinand, &req);
-	if (ret == -EOPNOTSUPP) {
-		/* Retry with ECC in case raw access is not supported */
-		req.mode = MTD_OPS_PLACE_OOB;
-		ret = spinand_write_page(spinand, &req);
+	for (i = 0; i < npages; i++) {
+		req.pos.page = i;
+		req.ooblen = marker_len;
+		req.mode = MTD_OPS_RAW;
+
+		res = spinand_write_page(spinand, &req);
+		if (res == -EOPNOTSUPP) {
+			/* Retry with ECC in case raw access is not supported */
+			req.mode = MTD_OPS_PLACE_OOB;
+			res = spinand_write_page(spinand, &req);
+		}
+		if (!ret)
+			ret = res;
 	}
 
 	return ret;
 }
 
 static int spinand_mtd_block_markbad(struct mtd_info *mtd, loff_t offs)
